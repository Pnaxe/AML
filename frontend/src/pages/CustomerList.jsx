import { useState } from 'react'
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
import axios from 'axios'
import { base_url } from '../services/api'

const CustomerList = () => {
    const [searchQuery, setSearchQuery] = useState('')
    const [riskFilter, setRiskFilter] = useState('')
    const [kycFilter, setKycFilter] = useState(null) // null = default (verified), '' = all, 'verified'/'pending'/'rejected' = specific
    const [isModalOpen, setIsModalOpen] = useState(false)
    const [isViewModalOpen, setIsViewModalOpen] = useState(false)
    const [isEditModalOpen, setIsEditModalOpen] = useState(false)
    const [isDeleteModalOpen, setIsDeleteModalOpen] = useState(false)
    const [selectedCustomer, setSelectedCustomer] = useState(null)
    const [showSuccessNotification, setShowSuccessNotification] = useState(false)
    const [currentStep, setCurrentStep] = useState(1) // 1: Customer Info, 2: KYC Verification, 3: Review
    const [kycData, setKycData] = useState({
        source_of_funds: '',
        source_of_wealth: '',
        notes: ''
    })
    const [screeningResults, setScreeningResults] = useState(null)
    const [isScreening, setIsScreening] = useState(false)
    const [tempCustomerId, setTempCustomerId] = useState(null) // Store customer ID before verification
    const [editFormData, setEditFormData] = useState({
        customer_type: 'INDIVIDUAL',
        customer_id: '',
        first_name: '',
        last_name: '',
        company_name: '',
        registration_number: '',
        email: '',
        phone_number: '',
        address: '',
        city: '',
        country: '',
        postal_code: '',
        risk_level: 'LOW',
        kyc_verified: false,  // KYC will be done separately after adding customer
        is_pep: false,
        is_sanctioned: false,
    })
    const [formData, setFormData] = useState({
        customer_type: 'INDIVIDUAL',
        // customer_id is auto-generated by the system
        first_name: '',
        last_name: '',
        company_name: '',
        registration_number: '',
        email: '',
        phone_number: '',
        address: '',
        city: '',
        country: '',
        postal_code: '',
        // risk_level is auto-calculated by the system based on screening results
        kyc_verified: false,  // KYC will be done separately after adding customer
        is_pep: false,
        is_sanctioned: false,
    })

    const queryClient = useQueryClient()

    // Mutation to create customer (unverified)
    const createCustomerMutation = useMutation({
        mutationFn: async (customerData) => {
            // Remove customer_id and risk_level from data as they're auto-generated
            const { customer_id, risk_level, ...dataToSend } = customerData
            const customerResponse = await axios.post(`${base_url}/customers/`, {
                ...dataToSend,
                kyc_verified: false
            })
            return customerResponse.data
        },
        onSuccess: (customer) => {
            setTempCustomerId(customer.id)
            // Update formData with generated customer_id for display in review
            setFormData(prev => ({ ...prev, customer_id: customer.customer_id }))
            // Invalidate pending KYC count since new unverified customer was created
            queryClient.invalidateQueries(['pending-kyc-count'])
        }
    })

    // Mutation to screen customer
    const screenCustomerMutation = useMutation({
        mutationFn: async (customerId) => {
            const response = await axios.post(`${base_url}/customers/${customerId}/screen/`)
            return response.data
        },
        onSuccess: (results) => {
            setScreeningResults(results)
            setIsScreening(false)
        },
        onError: () => {
            setIsScreening(false)
        }
    })

    // Mutation to accept customer
    const acceptCustomerMutation = useMutation({
        mutationFn: async (customerId) => {
            const response = await axios.post(`${base_url}/customers/${customerId}/accept/`)
            return response.data
        },
        onSuccess: () => {
            queryClient.invalidateQueries(['customers'])
            queryClient.invalidateQueries(['pending-kyc-count']) // Update sidebar badge
            setIsModalOpen(false)
            setCurrentStep(1)
            setTempCustomerId(null)
            setScreeningResults(null)
            setKycData({
                source_of_funds: '',
                source_of_wealth: '',
                notes: ''
            })
            setFormData({
                customer_type: 'INDIVIDUAL',
                // customer_id is auto-generated by the system
                first_name: '',
                last_name: '',
                company_name: '',
                registration_number: '',
                email: '',
                phone_number: '',
                address: '',
                city: '',
                country: '',
                postal_code: '',
                risk_level: 'LOW',
                kyc_verified: false,
                is_pep: false,
                is_sanctioned: false,
            })
            setShowSuccessNotification(true)
            setTimeout(() => setShowSuccessNotification(false), 3000)
        }
    })

    // Mutation to reject customer
    const rejectCustomerMutation = useMutation({
        mutationFn: async ({ customerId, reason }) => {
            const response = await axios.post(`${base_url}/customers/${customerId}/reject/`, {
                rejection_reason: reason
            })
            return response.data
        },
        onSuccess: () => {
            queryClient.invalidateQueries(['customers'])
            setIsModalOpen(false)
            setCurrentStep(1)
            setTempCustomerId(null)
            setScreeningResults(null)
            setKycData({
                source_of_funds: '',
                source_of_wealth: '',
                notes: ''
            })
            setFormData({
                customer_type: 'INDIVIDUAL',
                // customer_id is auto-generated by the system
                first_name: '',
                last_name: '',
                company_name: '',
                registration_number: '',
                email: '',
                phone_number: '',
                address: '',
                city: '',
                country: '',
                postal_code: '',
                risk_level: 'LOW',
                kyc_verified: false,
                is_pep: false,
                is_sanctioned: false,
            })
        }
    })

    // Cleanup function for modal close
    const resetModal = () => {
        setCurrentStep(1)
        setTempCustomerId(null)
        setScreeningResults(null)
        setIsScreening(false)
        setKycData({
            source_of_funds: '',
            source_of_wealth: '',
            notes: ''
        })
        setFormData({
            customer_type: 'INDIVIDUAL',
            // customer_id is auto-generated by the system
            first_name: '',
            last_name: '',
            company_name: '',
            registration_number: '',
            email: '',
            phone_number: '',
            address: '',
            city: '',
            country: '',
            postal_code: '',
            // risk_level is auto-calculated by the system
            kyc_verified: false,
            is_pep: false,
            is_sanctioned: false,
        })
    }

    const updateCustomerMutation = useMutation({
        mutationFn: ({ id, data }) => axios.put(`${base_url}/customers/${id}/`, data),
        onSuccess: () => {
            queryClient.invalidateQueries(['customers'])
            setIsEditModalOpen(false)
            setSelectedCustomer(null)
            setShowSuccessNotification(true)
            setTimeout(() => {
                setShowSuccessNotification(false)
            }, 3000) // Auto-dismiss after 3 seconds
        },
    })

    const deleteCustomerMutation = useMutation({
        mutationFn: (id) => axios.delete(`${base_url}/customers/${id}/`),
        onSuccess: (_, deletedId) => {
            // Optimistically remove from cache
            queryClient.setQueryData(['customers', searchQuery, riskFilter, kycFilter], (oldData) => {
                if (!oldData) return oldData
                return {
                    ...oldData,
                    results: oldData.results?.filter(customer => customer.id !== deletedId) || [],
                    count: (oldData.count || 0) - 1
                }
            })
            setIsDeleteModalOpen(false)
            setSelectedCustomer(null)
            setShowSuccessNotification(true)
            setTimeout(() => {
                setShowSuccessNotification(false)
            }, 3000) // Auto-dismiss after 3 seconds
        },
        onError: () => {
            // On error, refetch to ensure consistency
            queryClient.invalidateQueries(['customers'])
        },
    })

    const { data, isLoading, error } = useQuery({
        queryKey: ['customers', searchQuery, riskFilter, kycFilter],
        queryFn: async () => {
            // First, get customers with basic filters
            const params = {}
            if (searchQuery) params.search = searchQuery
            if (riskFilter) params.risk_level = riskFilter.toUpperCase()

            // For pending/rejected/all, we need all customers to check KYC profile status
            // For verified, use kyc_verified filter
            // For null (default), show verified
            if (kycFilter === 'pending' || kycFilter === 'rejected' || kycFilter === '') {
                params.page_size = 10000 // Get all to filter by KYC status
            } else if (kycFilter === 'verified') {
                params.kyc_verified = true
            } else {
                // Default to verified if no filter is set (null)
                params.kyc_verified = true
            }

            // Request 100 customers per page (unless filtering by status)
            if (!params.page_size) {
                params.page_size = 100
            }

            const response = await axios.get(`${base_url}/customers/`, { params })
            let customers = response.data.results || []

            // If filtering by pending or rejected, we need to check KYC profile status
            if (kycFilter === 'pending' || kycFilter === 'rejected') {
                // Fetch all KYC profiles to check their status
                const kycResponse = await axios.get(`${base_url}/kyc-profiles/`, {
                    params: { page_size: 10000 }
                })
                const kycProfiles = kycResponse.data.results || []

                // Create a map of customer ID to KYC profile status
                const customerKycStatus = {}
                kycProfiles.forEach(profile => {
                    if (profile.customer) {
                        customerKycStatus[profile.customer] = profile.verification_status
                    }
                })

                // Filter customers based on KYC profile status
                if (kycFilter === 'pending') {
                    // Pending: customers with PENDING or IN_PROGRESS status, or no KYC profile and kyc_verified is false
                    customers = customers.filter(customer => {
                        const status = customerKycStatus[customer.id]
                        return status === 'PENDING' || status === 'IN_PROGRESS' ||
                            (!status && !customer.kyc_verified)
                    })
                } else if (kycFilter === 'rejected') {
                    // Rejected: customers with REJECTED status
                    customers = customers.filter(customer => {
                        return customerKycStatus[customer.id] === 'REJECTED'
                    })
                }
            } else if (kycFilter === '') {
                // All Status: show all customers (already fetched with page_size 10000)
                // No additional filtering needed
            }

            return {
                results: customers,
                count: customers.length
            }
        },
    })

    const customers = data?.results || []

    const getRiskBadgeClass = (riskLevel) => {
        switch (riskLevel) {
            case 'HIGH':
            case 'CRITICAL':
                return 'bg-red-100 text-red-700'
            case 'MEDIUM':
                return 'bg-yellow-100 text-yellow-700'
            default:
                return 'bg-green-100 text-green-700'
        }
    }

    return (
        <div className="flex flex-col flex-1 min-h-0 overflow-hidden">
            {/* Page Title */}
            <div className="mb-4 flex items-center justify-between">
                <div>
                    <h4 className="text-base font-bold text-gray-900">Customer Management & KYC Verification</h4>
                    <small className="text-gray-500 text-xs">Add customers with KYC verification - only verified customers are shown</small>
                </div>
                <div>
                    <button
                        onClick={() => {
                            // Reset form
                            setFormData({
                                customer_type: 'INDIVIDUAL',
                                customer_id: '',
                                first_name: '',
                                last_name: '',
                                company_name: '',
                                registration_number: '',
                                email: '',
                                phone_number: '',
                                address: '',
                                city: '',
                                country: '',
                                postal_code: '',
                                risk_level: 'LOW',
                                kyc_verified: false,  // KYC will be done separately
                                is_pep: false,
                                is_sanctioned: false,
                            })
                            setIsModalOpen(true)
                        }}
                        className="bg-blue-600 text-white px-4 py-2 hover:bg-blue-700 transition-all duration-200 flex items-center space-x-2 font-medium"
                    >
                        <svg className="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                            <path fillRule="evenodd" d="M10 3a1 1 0 011 1v5h5a1 1 0 110 2h-5v5a1 1 0 11-2 0v-5H4a1 1 0 110-2h5V4a1 1 0 011-1z" clipRule="evenodd"></path>
                        </svg>
                        <span>Add New Customer</span>
                    </button>
                </div>
            </div>
            {/* Search and Filters */}
            <div className="bg-gray-50 border border-gray-200 p-4 -mx-2 flex-shrink-0 mb-3">
                <div className="flex items-center justify-between">
                    <div className="flex items-center">
                        {/* Search Bar */}
                        <div className="relative">
                            <input
                                type="text"
                                placeholder="Search customers..."
                                value={searchQuery}
                                onChange={(e) => setSearchQuery(e.target.value)}
                                className="w-64 px-4 py-2 border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 text-sm bg-white"
                            />
                        </div>

                        {/* KYC Status Filter */}
                        <div className="flex items-center space-x-2 ml-4">
                            <label className="text-xs font-medium text-gray-700">KYC Status:</label>
                            <select
                                value={kycFilter === null ? '' : (kycFilter || '')}
                                onChange={(e) => {
                                    const value = e.target.value
                                    // If "All Status" is selected, set to '' (empty string means show all)
                                    // If null (initial), it means use default (verified)
                                    setKycFilter(value === '' ? '' : value)
                                }}
                                className="px-3 py-1 border border-gray-300 text-xs focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 w-auto min-w-[120px]"
                            >
                                <option value="">All Status</option>
                                <option value="verified">Verified</option>
                                <option value="pending">Pending</option>
                                <option value="rejected">Rejected</option>
                            </select>
                        </div>

                        {/* Risk Level Filter */}
                        <div className="flex items-center space-x-2 ml-4">
                            <label className="text-xs font-medium text-gray-700">Risk Level:</label>
                            <select
                                value={riskFilter}
                                onChange={(e) => setRiskFilter(e.target.value)}
                                className="px-3 py-1 border border-gray-300 text-xs focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 w-auto min-w-[140px]"
                            >
                                <option value="">All Risk Levels</option>
                                <option value="high">High</option>
                                <option value="medium">Medium</option>
                                <option value="low">Low</option>
                            </select>
                        </div>
                    </div>

                    {/* Action Buttons */}
                    <div className="flex items-center space-x-3">

                    </div>
                </div>
            </div>

            {/* Customer Table */}
            <div className="bg-white flex flex-col border border-gray-200 overflow-hidden flex-1 min-h-0">
                <div className="flex-1 overflow-y-auto overflow-x-auto">

                    {/* Scrollable Table Body */}
                    {isLoading ? (
                        <div className="flex items-center justify-center h-full">
                            <div className="text-gray-500">Loading...</div>
                        </div>
                    ) : error ? (
                        <div className="flex flex-col items-center justify-center h-full space-y-2">
                            <div className="text-red-500 font-semibold">Error loading customers</div>
                            <div className="text-xs text-gray-600">
                                {error.response?.status === 401
                                    ? 'Authentication required. Please log in to the Django admin first.'
                                    : error.response?.status === 403
                                        ? 'Permission denied. You do not have access to this resource.'
                                        : error.response?.status
                                            ? `Error ${error.response.status}: ${error.response.statusText}`
                                            : error.message || 'Network error. Please check if the Django server is running.'}
                            </div>
                            {error.response?.status === 401 && (
                                <a
                                    href="http://localhost:8000/admin/login/"
                                    target="_blank"
                                    className="text-xs text-blue-600 hover:text-blue-800 underline mt-2"
                                >
                                    Open Django Admin Login
                                </a>
                            )}
                        </div>
                    ) : (
                        <table className="w-full border-collapse">
                            <thead className="bg-gray-700 sticky top-0 z-10">
                                <tr className="border-b border-gray-200">
                                    <th className="px-2 md:px-3 py-1.5 md:py-2 text-left text-xs font-black uppercase tracking-wider whitespace-nowrap border-r border-gray-300" style={{ color: '#f3f4f6' }}>
                                        CUSTOMER ID
                                    </th>
                                    <th className="px-2 md:px-3 py-1.5 md:py-2 text-left text-xs font-black uppercase tracking-wider whitespace-nowrap border-r border-gray-300" style={{ color: '#f3f4f6' }}>
                                        NAME
                                    </th>
                                    <th className="px-2 md:px-3 py-1.5 md:py-2 text-left text-xs font-black uppercase tracking-wider whitespace-nowrap border-r border-gray-300" style={{ color: '#f3f4f6' }}>
                                        EMAIL
                                    </th>
                                    <th className="px-2 md:px-3 py-1.5 md:py-2 text-left text-xs font-black uppercase tracking-wider whitespace-nowrap border-r border-gray-300" style={{ color: '#f3f4f6' }}>
                                        RISK LEVEL
                                    </th>
                                    <th className="px-2 md:px-3 py-1.5 md:py-2 text-left text-xs font-black uppercase tracking-wider whitespace-nowrap border-r border-gray-300" style={{ color: '#f3f4f6' }}>
                                        KYC STATUS
                                    </th>
                                    <th className="px-2 md:px-3 py-1.5 md:py-2 text-left text-xs font-black uppercase tracking-wider whitespace-nowrap border-r border-gray-300" style={{ color: '#f3f4f6' }}>
                                        LAST ACTIVITY
                                    </th>
                                    <th className="px-2 md:px-3 py-1.5 md:py-2 text-left text-xs font-black uppercase tracking-wider whitespace-nowrap" style={{ color: '#f3f4f6' }}>
                                        ACTIONS
                                    </th>
                                </tr>
                            </thead>
                            <tbody className="bg-white">
                                {Array.from({ length: Math.max(100, customers.length) }).map((_, index) => {
                                    const customer = customers[index];
                                    return (
                                        <tr key={customer?.id || index} className={`border-b border-gray-200 ${index % 2 === 0 ? 'bg-gray-100' : 'bg-gray-50'} ${customer ? 'hover:opacity-80' : ''}`} style={{ minHeight: '36px' }}>
                                            <td className="px-2 md:px-3 py-1.5 md:py-2 text-xs font-mono text-gray-900 whitespace-nowrap border-r border-gray-200" style={{ minHeight: '36px' }}>
                                                {customer?.customer_id || '\u00A0'}
                                            </td>
                                            <td className="px-2 md:px-3 py-1.5 md:py-2 text-xs text-gray-900 whitespace-nowrap border-r border-gray-200" style={{ minHeight: '36px' }}>
                                                {customer ? (
                                                    customer.customer_type === 'INDIVIDUAL'
                                                        ? `${customer.first_name || ''} ${customer.last_name || ''}`.trim() || 'N/A'
                                                        : customer.company_name || 'N/A'
                                                ) : '\u00A0'}
                                            </td>
                                            <td className="px-2 md:px-3 py-1.5 md:py-2 text-xs text-gray-600 whitespace-nowrap truncate border-r border-gray-200" style={{ minHeight: '36px' }}>
                                                {customer?.email || '\u00A0'}
                                            </td>
                                            <td className="px-2 md:px-3 py-1.5 md:py-2 whitespace-nowrap border-r border-gray-200" style={{ minHeight: '36px' }}>
                                                {customer ? (
                                                    <span className={`px-2 py-1 rounded-full text-xs ${getRiskBadgeClass(customer.risk_level)}`}>
                                                        {customer.risk_level || 'LOW'}
                                                    </span>
                                                ) : '\u00A0'}
                                            </td>
                                            <td className="px-2 md:px-3 py-1.5 md:py-2 whitespace-nowrap border-r border-gray-200" style={{ minHeight: '36px' }}>
                                                {customer ? (
                                                    <span className={`px-2 py-1 rounded-full text-xs ${customer.kyc_verified
                                                        ? 'bg-green-100 text-green-700'
                                                        : 'bg-yellow-100 text-yellow-700'
                                                        }`}>
                                                        {customer.kyc_verified ? 'Verified' : 'Pending'}
                                                    </span>
                                                ) : '\u00A0'}
                                            </td>
                                            <td className="px-2 md:px-3 py-1.5 md:py-2 text-xs text-gray-500 whitespace-nowrap border-r border-gray-200" style={{ minHeight: '36px' }}>
                                                {customer?.updated_at ? new Date(customer.updated_at).toLocaleDateString() : '\u00A0'}
                                            </td>
                                            <td className="px-2 md:px-3 py-1.5 md:py-2 whitespace-nowrap" style={{ minHeight: '36px' }}>
                                                {customer ? (
                                                    <div className="flex items-center space-x-2">
                                                        <button
                                                            onClick={() => {
                                                                setSelectedCustomer(customer)
                                                                setIsViewModalOpen(true)
                                                            }}
                                                            className="text-blue-600 hover:text-blue-800 text-xs font-medium"
                                                        >
                                                            View
                                                        </button>
                                                        <button className="text-gray-400">|</button>
                                                        <button
                                                            onClick={() => {
                                                                setSelectedCustomer(customer)
                                                                setEditFormData({
                                                                    customer_type: customer.customer_type || 'INDIVIDUAL',
                                                                    customer_id: customer.customer_id || '',
                                                                    first_name: customer.first_name || '',
                                                                    last_name: customer.last_name || '',
                                                                    company_name: customer.company_name || '',
                                                                    registration_number: customer.registration_number || '',
                                                                    email: customer.email || '',
                                                                    phone_number: customer.phone_number || '',
                                                                    address: customer.address || '',
                                                                    city: customer.city || '',
                                                                    country: customer.country || '',
                                                                    postal_code: customer.postal_code || '',
                                                                    risk_level: customer.risk_level || 'LOW',
                                                                    kyc_verified: customer.kyc_verified || false,
                                                                    is_pep: customer.is_pep || false,
                                                                    is_sanctioned: customer.is_sanctioned || false,
                                                                })
                                                                setIsEditModalOpen(true)
                                                            }}
                                                            className="text-gray-600 hover:text-gray-800 text-xs font-medium"
                                                        >
                                                            Edit
                                                        </button>
                                                        <button className="text-gray-400">|</button>
                                                        <button
                                                            onClick={() => {
                                                                setSelectedCustomer(customer)
                                                                setIsDeleteModalOpen(true)
                                                            }}
                                                            className="text-red-600 hover:text-red-800 text-xs font-medium"
                                                        >
                                                            Delete
                                                        </button>
                                                    </div>
                                                ) : '\u00A0'}
                                            </td>
                                        </tr>
                                    );
                                })}
                            </tbody>
                        </table>
                    )}
                </div>
            </div>

            {/* Paginator */}
            <div className="bg-white p-4 border border-gray-200 flex items-center justify-start flex-shrink-0 mt-1 mb-1 w-full" style={{
                boxShadow: '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
                borderBottom: '1px solid rgba(0, 0, 0, 0.1)'
            }}>
                <div className="text-sm text-gray-700">
                    {data?.count ? `Showing ${customers.length} of ${data.count} customers` : customers.length > 0 ? `Showing ${customers.length} customers` : 'No customers found'}
                </div>
            </div>

            {/* Success Notification */}
            {showSuccessNotification && (
                <div className="fixed top-4 right-4 z-50 animate-fade-in">
                    <div className="bg-green-500 text-white px-6 py-4 rounded-md shadow-lg flex items-center space-x-3 min-w-[300px]">
                        <svg className="w-6 h-6 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M5 13l4 4L19 7"></path>
                        </svg>
                        <div className="flex-1">
                            <p className="font-semibold">Success!</p>
                            <p className="text-sm">Customer {deleteCustomerMutation.isSuccess ? 'deleted' : 'updated'} successfully.</p>
                        </div>
                        <button
                            onClick={() => setShowSuccessNotification(false)}
                            className="ml-2 text-white hover:text-gray-200 flex-shrink-0"
                        >
                            <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M6 18L18 6M6 6l12 12"></path>
                            </svg>
                        </button>
                    </div>
                </div>
            )}

            {/* Delete Confirmation Modal */}
            {isDeleteModalOpen && selectedCustomer && (
                <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-[60]">
                    <div className="bg-white shadow-xl w-full max-w-md m-4 flex flex-col">
                        {/* Header - Static */}
                        <div className="bg-white border-b border-gray-200 px-6 py-4 flex items-center justify-between flex-shrink-0">
                            <h3 className="text-lg font-semibold text-gray-900">Delete Customer</h3>
                            <button
                                onClick={() => {
                                    setIsDeleteModalOpen(false)
                                    setSelectedCustomer(null)
                                }}
                                className="text-gray-400 hover:text-gray-600"
                            >
                                <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M6 18L18 6M6 6l12 12"></path>
                                </svg>
                            </button>
                        </div>

                        {/* Content */}
                        <div className="flex-1 overflow-y-auto p-6">
                            <div className="flex items-start space-x-4">
                                <div className="flex-shrink-0">
                                    <div className="w-12 h-12 bg-red-100 rounded-full flex items-center justify-center">
                                        <svg className="w-6 h-6 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
                                        </svg>
                                    </div>
                                </div>
                                <div className="flex-1">
                                    <p className="text-sm text-gray-700 mb-2">
                                        Are you sure you want to delete this customer? This will perform a soft delete and mark the customer as inactive.
                                    </p>
                                    <div className="bg-gray-50 p-3 rounded-md mt-3">
                                        <p className="text-xs font-medium text-gray-500 mb-1">Customer ID:</p>
                                        <p className="text-sm font-mono text-gray-900">{selectedCustomer.customer_id}</p>
                                        <p className="text-xs font-medium text-gray-500 mb-1 mt-2">Name:</p>
                                        <p className="text-sm text-gray-900">
                                            {selectedCustomer.customer_type === 'INDIVIDUAL'
                                                ? `${selectedCustomer.first_name || ''} ${selectedCustomer.last_name || ''}`.trim() || 'N/A'
                                                : selectedCustomer.company_name || 'N/A'}
                                        </p>
                                    </div>
                                </div>
                            </div>

                            {/* Error Message */}
                            {deleteCustomerMutation.isError && (
                                <div className="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded text-sm mt-4">
                                    {deleteCustomerMutation.error?.response?.data?.detail ||
                                        deleteCustomerMutation.error?.message ||
                                        'Failed to delete customer. Please try again.'}
                                </div>
                            )}
                        </div>

                        {/* Footer - Static */}
                        <div className="bg-white border-t border-gray-200 px-6 py-4 flex justify-end space-x-3 flex-shrink-0">
                            <button
                                type="button"
                                onClick={() => {
                                    setIsDeleteModalOpen(false)
                                    setSelectedCustomer(null)
                                }}
                                className="px-3 py-1.5 text-xs font-medium text-gray-700 bg-white border border-gray-300 rounded-md hover:bg-gray-50"
                            >
                                Cancel
                            </button>
                            <button
                                type="button"
                                onClick={() => {
                                    const customerId = selectedCustomer.id
                                    deleteCustomerMutation.mutate(customerId)
                                }}
                                disabled={deleteCustomerMutation.isLoading}
                                className="px-3 py-1.5 text-xs font-medium text-white bg-red-600 rounded-md hover:bg-red-700 disabled:opacity-50 disabled:cursor-not-allowed"
                            >
                                {deleteCustomerMutation.isLoading ? 'Deleting...' : 'Delete Customer'}
                            </button>
                        </div>
                    </div>
                </div>
            )}

            {/* View Customer Modal */}
            {isViewModalOpen && selectedCustomer && (
                <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-[60]">
                    <div className="bg-white shadow-xl w-full max-w-2xl max-h-[70vh] m-4 flex flex-col">
                        {/* Header - Static */}
                        <div className="bg-white border-b border-gray-200 shadow px-6 py-4 flex items-center justify-between flex-shrink-0">
                            <h3 className="text-lg font-semibold text-gray-900">Customer Details</h3>
                            <button
                                onClick={() => {
                                    setIsViewModalOpen(false)
                                    setSelectedCustomer(null)
                                }}
                                className="text-gray-400 hover:text-gray-600"
                            >
                                <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M6 18L18 6M6 6l12 12"></path>
                                </svg>
                            </button>
                        </div>

                        {/* Scrollable Content */}
                        <div className="flex-1 overflow-y-auto p-6 space-y-4">
                            {/* Customer Information Section */}
                            <div className="bg-gray-50 border border-gray-200 rounded-md p-4">
                                <h4 className="text-sm font-semibold text-gray-900 mb-3">Customer Information</h4>
                                <div className="grid grid-cols-2 gap-3 text-xs">
                                    <div>
                                        <span className="font-medium text-gray-600">Customer ID:</span>
                                        <p className="text-gray-900 font-mono">{selectedCustomer.customer_id || 'N/A'}</p>
                                    </div>
                                    <div>
                                        <span className="font-medium text-gray-600">Customer Type:</span>
                                        <p className="text-gray-900">{selectedCustomer.customer_type || 'N/A'}</p>
                                    </div>
                                    {selectedCustomer.customer_type === 'INDIVIDUAL' ? (
                                        <>
                                            <div>
                                                <span className="font-medium text-gray-600">First Name:</span>
                                                <p className="text-gray-900">{selectedCustomer.first_name || 'N/A'}</p>
                                            </div>
                                            <div>
                                                <span className="font-medium text-gray-600">Last Name:</span>
                                                <p className="text-gray-900">{selectedCustomer.last_name || 'N/A'}</p>
                                            </div>
                                        </>
                                    ) : (
                                        <>
                                            <div>
                                                <span className="font-medium text-gray-600">Company Name:</span>
                                                <p className="text-gray-900">{selectedCustomer.company_name || 'N/A'}</p>
                                            </div>
                                            {selectedCustomer.registration_number && (
                                                <div>
                                                    <span className="font-medium text-gray-600">Registration Number:</span>
                                                    <p className="text-gray-900">{selectedCustomer.registration_number}</p>
                                                </div>
                                            )}
                                        </>
                                    )}
                                    <div>
                                        <span className="font-medium text-gray-600">Email:</span>
                                        <p className="text-gray-900">{selectedCustomer.email || 'N/A'}</p>
                                    </div>
                                    <div>
                                        <span className="font-medium text-gray-600">Phone Number:</span>
                                        <p className="text-gray-900">{selectedCustomer.phone_number || 'N/A'}</p>
                                    </div>
                                    <div>
                                        <span className="font-medium text-gray-600">Address:</span>
                                        <p className="text-gray-900">{selectedCustomer.address || 'N/A'}</p>
                                    </div>
                                    <div>
                                        <span className="font-medium text-gray-600">City:</span>
                                        <p className="text-gray-900">{selectedCustomer.city || 'N/A'}</p>
                                    </div>
                                    <div>
                                        <span className="font-medium text-gray-600">Country:</span>
                                        <p className="text-gray-900">{selectedCustomer.country || 'N/A'}</p>
                                    </div>
                                    {selectedCustomer.postal_code && (
                                        <div>
                                            <span className="font-medium text-gray-600">Postal Code:</span>
                                            <p className="text-gray-900">{selectedCustomer.postal_code}</p>
                                        </div>
                                    )}
                                    <div>
                                        <span className="font-medium text-gray-600">Risk Level:</span>
                                        <p className={`font-semibold ${
                                            selectedCustomer.risk_level === 'HIGH' || selectedCustomer.risk_level === 'CRITICAL' ? 'text-red-700' :
                                            selectedCustomer.risk_level === 'MEDIUM' ? 'text-yellow-700' :
                                            'text-green-700'
                                        }`}>
                                            {selectedCustomer.risk_level || 'N/A'}
                                        </p>
                                    </div>
                                    <div>
                                        <span className="font-medium text-gray-600">KYC Verified:</span>
                                        <p className={selectedCustomer.kyc_verified ? 'text-green-600 font-semibold' : 'text-yellow-600 font-semibold'}>
                                            {selectedCustomer.kyc_verified ? 'Yes' : 'No'}
                                        </p>
                                    </div>
                                    {selectedCustomer.is_pep && (
                                        <div>
                                            <span className="font-medium text-gray-600">PEP Status:</span>
                                            <p className="text-red-600 font-semibold">Politically Exposed Person</p>
                                        </div>
                                    )}
                                    {selectedCustomer.is_sanctioned && (
                                        <div>
                                            <span className="font-medium text-gray-600">Sanctions Status:</span>
                                            <p className="text-red-600 font-semibold">Sanctioned</p>
                                        </div>
                                    )}
                                </div>
                            </div>
                        </div>

                        {/* Footer - Static */}
                        <div className="bg-white border-t border-gray-200 shadow px-3 py-2 flex justify-end space-x-3 flex-shrink-0" style={{
                            boxShadow: '0 -4px 6px -1px rgba(0, 0, 0, 0.1), 0 -2px 4px -1px rgba(0, 0, 0, 0.06)'
                        }}>
                            <button
                                type="button"
                                onClick={() => {
                                    setIsViewModalOpen(false)
                                    setSelectedCustomer(null)
                                }}
                                className="px-3 py-1.5 text-xs font-medium text-gray-700 bg-white border border-gray-300 rounded-md hover:bg-gray-50"
                            >
                                Close
                            </button>
                        </div>
                    </div>
                </div>
            )}

            {/* Edit Customer Modal */}
            {isEditModalOpen && selectedCustomer && (
                <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-[60]">
                    <div className="bg-white shadow-xl w-full max-w-2xl max-h-[70vh] m-4 flex flex-col">
                        {/* Header - Static */}
                        <div className="bg-white border-b border-gray-200 px-6 py-4 flex items-center justify-between flex-shrink-0">
                            <h3 className="text-lg font-semibold text-gray-900">Edit Customer</h3>
                            <button
                                onClick={() => {
                                    setIsEditModalOpen(false)
                                    setSelectedCustomer(null)
                                }}
                                className="text-gray-400 hover:text-gray-600"
                            >
                                <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M6 18L18 6M6 6l12 12"></path>
                                </svg>
                            </button>
                        </div>

                        {/* Scrollable Content */}
                        <form onSubmit={(e) => {
                            e.preventDefault()
                            updateCustomerMutation.mutate({ id: selectedCustomer.id, data: editFormData })
                        }} className="flex-1 overflow-y-auto p-6 space-y-4">
                            {/* Customer Type */}
                            <div>
                                <label className="block text-sm font-medium text-gray-700 mb-1">Customer Type</label>
                                <select
                                    value={editFormData.customer_type}
                                    onChange={(e) => setEditFormData({ ...editFormData, customer_type: e.target.value })}
                                    className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                    required
                                >
                                    <option value="INDIVIDUAL">Individual</option>
                                    <option value="CORPORATE">Corporate</option>
                                </select>
                            </div>

                            {/* Customer ID */}
                            <div>
                                <label className="block text-sm font-medium text-gray-700 mb-1">Customer ID</label>
                                <input
                                    type="text"
                                    value={editFormData.customer_id}
                                    onChange={(e) => setEditFormData({ ...editFormData, customer_id: e.target.value })}
                                    className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                    required
                                />
                            </div>

                            {/* Individual Fields */}
                            {editFormData.customer_type === 'INDIVIDUAL' && (
                                <>
                                    <div className="grid grid-cols-2 gap-4">
                                        <div>
                                            <label className="block text-sm font-medium text-gray-700 mb-1">First Name</label>
                                            <input
                                                type="text"
                                                value={editFormData.first_name}
                                                onChange={(e) => setEditFormData({ ...editFormData, first_name: e.target.value })}
                                                className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                                required
                                            />
                                        </div>
                                        <div>
                                            <label className="block text-sm font-medium text-gray-700 mb-1">Last Name</label>
                                            <input
                                                type="text"
                                                value={editFormData.last_name}
                                                onChange={(e) => setEditFormData({ ...editFormData, last_name: e.target.value })}
                                                className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                                required
                                            />
                                        </div>
                                    </div>
                                </>
                            )}

                            {/* Corporate Fields */}
                            {editFormData.customer_type === 'CORPORATE' && (
                                <>
                                    <div>
                                        <label className="block text-sm font-medium text-gray-700 mb-1">Company Name</label>
                                        <input
                                            type="text"
                                            value={editFormData.company_name}
                                            onChange={(e) => setEditFormData({ ...editFormData, company_name: e.target.value })}
                                            className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                            required
                                        />
                                    </div>
                                    <div>
                                        <label className="block text-sm font-medium text-gray-700 mb-1">Registration Number</label>
                                        <input
                                            type="text"
                                            value={editFormData.registration_number}
                                            onChange={(e) => setEditFormData({ ...editFormData, registration_number: e.target.value })}
                                            className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                        />
                                    </div>
                                </>
                            )}

                            {/* Contact Information */}
                            <div className="grid grid-cols-2 gap-4">
                                <div>
                                    <label className="block text-sm font-medium text-gray-700 mb-1">Email</label>
                                    <input
                                        type="email"
                                        value={editFormData.email}
                                        onChange={(e) => setEditFormData({ ...editFormData, email: e.target.value })}
                                        className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                        required
                                    />
                                </div>
                                <div>
                                    <label className="block text-sm font-medium text-gray-700 mb-1">Phone Number</label>
                                    <input
                                        type="tel"
                                        value={editFormData.phone_number}
                                        onChange={(e) => setEditFormData({ ...editFormData, phone_number: e.target.value })}
                                        className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                    />
                                </div>
                            </div>

                            {/* Address */}
                            <div>
                                <label className="block text-sm font-medium text-gray-700 mb-1">Address</label>
                                <input
                                    type="text"
                                    value={editFormData.address}
                                    onChange={(e) => setEditFormData({ ...editFormData, address: e.target.value })}
                                    className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                />
                            </div>

                            <div className="grid grid-cols-3 gap-4">
                                <div>
                                    <label className="block text-sm font-medium text-gray-700 mb-1">City</label>
                                    <input
                                        type="text"
                                        value={editFormData.city}
                                        onChange={(e) => setEditFormData({ ...editFormData, city: e.target.value })}
                                        className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                    />
                                </div>
                                <div>
                                    <label className="block text-sm font-medium text-gray-700 mb-1">Country</label>
                                    <input
                                        type="text"
                                        value={editFormData.country}
                                        onChange={(e) => setEditFormData({ ...editFormData, country: e.target.value })}
                                        className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                    />
                                </div>
                                <div>
                                    <label className="block text-sm font-medium text-gray-700 mb-1">Postal Code</label>
                                    <input
                                        type="text"
                                        value={editFormData.postal_code}
                                        onChange={(e) => setEditFormData({ ...editFormData, postal_code: e.target.value })}
                                        className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                    />
                                </div>
                            </div>

                            {/* Risk Level */}
                            <div>
                                <label className="block text-sm font-medium text-gray-700 mb-1">Risk Level</label>
                                <select
                                    value={editFormData.risk_level}
                                    onChange={(e) => setEditFormData({ ...editFormData, risk_level: e.target.value })}
                                    className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                    required
                                >
                                    <option value="LOW">Low</option>
                                    <option value="MEDIUM">Medium</option>
                                    <option value="HIGH">High</option>
                                    <option value="CRITICAL">Critical</option>
                                </select>
                            </div>

                            {/* Checkboxes */}
                            <div className="space-y-2">
                                <label className="flex items-center">
                                    <input
                                        type="checkbox"
                                        checked={editFormData.kyc_verified}
                                        onChange={(e) => setEditFormData({ ...editFormData, kyc_verified: e.target.checked })}
                                        className="mr-2"
                                    />
                                    <span className="text-sm text-gray-700">KYC Verified</span>
                                </label>
                                <label className="flex items-center">
                                    <input
                                        type="checkbox"
                                        checked={editFormData.is_pep}
                                        onChange={(e) => setEditFormData({ ...editFormData, is_pep: e.target.checked })}
                                        className="mr-2"
                                    />
                                    <span className="text-sm text-gray-700">PEP (Politically Exposed Person)</span>
                                </label>
                                <label className="flex items-center">
                                    <input
                                        type="checkbox"
                                        checked={editFormData.is_sanctioned}
                                        onChange={(e) => setEditFormData({ ...editFormData, is_sanctioned: e.target.checked })}
                                        className="mr-2"
                                    />
                                    <span className="text-sm text-gray-700">Sanctioned</span>
                                </label>
                            </div>

                            {/* Error Message */}
                            {updateCustomerMutation.isError && (
                                <div className="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded text-sm">
                                    {updateCustomerMutation.error?.response?.data?.detail ||
                                        updateCustomerMutation.error?.message ||
                                        'Failed to update customer. Please try again.'}
                                </div>
                            )}
                        </form>

                        {/* Footer - Static */}
                        <div className="bg-white border-t border-gray-200 px-6 py-4 flex justify-end space-x-3 flex-shrink-0">
                            <button
                                type="button"
                                onClick={() => {
                                    setIsEditModalOpen(false)
                                    setSelectedCustomer(null)
                                }}
                                className="px-3 py-1.5 text-xs font-medium text-gray-700 bg-white border border-gray-300 rounded-md hover:bg-gray-50"
                            >
                                Cancel
                            </button>
                            <button
                                type="button"
                                onClick={(e) => {
                                    e.preventDefault()
                                    updateCustomerMutation.mutate({ id: selectedCustomer.id, data: editFormData })
                                }}
                                disabled={updateCustomerMutation.isLoading}
                                className="px-3 py-1.5 text-xs font-medium text-white bg-blue-600 rounded-md hover:bg-blue-700 disabled:opacity-50 disabled:cursor-not-allowed"
                            >
                                {updateCustomerMutation.isLoading ? 'Updating...' : 'Update Customer'}
                            </button>
                        </div>
                    </div>
                </div>
            )}

            {/* Add Customer with KYC Modal */}
            {isModalOpen && (
                <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-[60]">
                    <div className="bg-white shadow-xl w-full max-w-2xl max-h-[70vh] m-4 flex flex-col">
                        {/* Header - Static */}
                        <div className="bg-white border-b border-gray-200 shadow px-3 py-2 flex items-center justify-between flex-shrink-0">
                            <div>
                                {/* <h3 className="text-lg font-semibold text-gray-900">Add New Customer with KYC</h3> */}
                                <div className="flex items-center space-x-2 mt-1">
                                    <div className={`w-8 h-8 rounded-full flex items-center justify-center text-xs font-semibold ${currentStep >= 1 ? 'bg-blue-600 text-white' : 'bg-gray-200 text-gray-600'}`}>
                                        1
                                    </div>
                                    <div className={`h-0.5 w-8 ${currentStep >= 2 ? 'bg-blue-600' : 'bg-gray-200'}`}></div>
                                    <div className={`w-8 h-8 rounded-full flex items-center justify-center text-xs font-semibold ${currentStep >= 2 ? 'bg-blue-600 text-white' : 'bg-gray-200 text-gray-600'}`}>
                                        2
                                    </div>
                                    <div className={`h-0.5 w-8 ${currentStep >= 3 ? 'bg-blue-600' : 'bg-gray-200'}`}></div>
                                    <div className={`w-8 h-8 rounded-full flex items-center justify-center text-xs font-semibold ${currentStep >= 3 ? 'bg-blue-600 text-white' : 'bg-gray-200 text-gray-600'}`}>
                                        3
                                    </div>
                                    <span className="ml-2 text-xs text-gray-600">
                                        {currentStep === 1 ? 'Customer Info' : currentStep === 2 ? 'KYC Verification' : 'Review'}
                                    </span>
                                </div>
                            </div>
                            <button
                                onClick={() => {
                                    setIsModalOpen(false)
                                    setCurrentStep(1)
                                    setKycData({
                                        source_of_funds: '',
                                        source_of_wealth: '',
                                        sanctions_screened: false,
                                        pep_checked: false,
                                        adverse_media_checked: false,
                                        document_verified: false,
                                        identity_verified: false,
                                        address_verified: false,
                                        notes: ''
                                    })
                                }}
                                className="text-gray-400 hover:text-gray-600"
                            >
                                <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M6 18L18 6M6 6l12 12"></path>
                                </svg>
                            </button>
                        </div>

                        {/* Scrollable Content */}
                        <div className="flex-1 overflow-y-auto p-6 space-y-4">
                            {/* Step 1: Customer Information */}
                            {currentStep === 1 && (
                                <div className="space-y-4">
                                    {/* Customer Type */}
                                    <div>
                                        <label className="block text-sm font-medium text-gray-700 mb-1">Customer Type</label>
                                        <select
                                            value={formData.customer_type}
                                            onChange={(e) => setFormData({ ...formData, customer_type: e.target.value })}
                                            className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                            required
                                        >
                                            <option value="INDIVIDUAL">Individual</option>
                                            <option value="CORPORATE">Corporate</option>
                                        </select>
                                    </div>

                                    {/* Customer ID - Auto-generated */}
                                    <div className="bg-blue-50 border border-blue-200 rounded-md p-3 mb-4">
                                        <p className="text-xs text-blue-800">
                                            <span className="font-semibold">Note:</span> Customer ID will be automatically generated by the system (e.g., CUST-IND-0001 or CUST-CORP-0001).
                                        </p>
                                    </div>

                                    {/* Individual Fields */}
                                    {formData.customer_type === 'INDIVIDUAL' && (
                                        <>
                                            <div className="grid grid-cols-2 gap-4">
                                                <div>
                                                    <label className="block text-sm font-medium text-gray-700 mb-1">
                                                        First Name <span className="text-red-500">*</span>
                                                    </label>
                                                    <input
                                                        type="text"
                                                        value={formData.first_name}
                                                        onChange={(e) => setFormData({ ...formData, first_name: e.target.value })}
                                                        className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                                        required
                                                    />
                                                </div>
                                                <div>
                                                    <label className="block text-sm font-medium text-gray-700 mb-1">
                                                        Last Name <span className="text-red-500">*</span>
                                                    </label>
                                                    <input
                                                        type="text"
                                                        value={formData.last_name}
                                                        onChange={(e) => setFormData({ ...formData, last_name: e.target.value })}
                                                        className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                                        required
                                                    />
                                                </div>
                                            </div>
                                        </>
                                    )}

                                    {/* Corporate Fields */}
                                    {formData.customer_type === 'CORPORATE' && (
                                        <>
                                            <div>
                                                <label className="block text-sm font-medium text-gray-700 mb-1">
                                                    Company Name <span className="text-red-500">*</span>
                                                </label>
                                                <input
                                                    type="text"
                                                    value={formData.company_name}
                                                    onChange={(e) => setFormData({ ...formData, company_name: e.target.value })}
                                                    className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                                    required
                                                />
                                            </div>
                                            <div>
                                                <label className="block text-sm font-medium text-gray-700 mb-1">Registration Number</label>
                                                <input
                                                    type="text"
                                                    value={formData.registration_number}
                                                    onChange={(e) => setFormData({ ...formData, registration_number: e.target.value })}
                                                    className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                                />
                                            </div>
                                        </>
                                    )}

                                    {/* Contact Information */}
                                    <div className="grid grid-cols-2 gap-4">
                                        <div>
                                            <label className="block text-sm font-medium text-gray-700 mb-1">
                                                Email <span className="text-red-500">*</span>
                                            </label>
                                            <input
                                                type="email"
                                                value={formData.email}
                                                onChange={(e) => setFormData({ ...formData, email: e.target.value })}
                                                className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                                required
                                            />
                                        </div>
                                        <div>
                                            <label className="block text-sm font-medium text-gray-700 mb-1">Phone Number</label>
                                            <input
                                                type="tel"
                                                value={formData.phone_number}
                                                onChange={(e) => setFormData({ ...formData, phone_number: e.target.value })}
                                                className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                            />
                                        </div>
                                    </div>

                                    {/* Address */}
                                    <div>
                                        <label className="block text-sm font-medium text-gray-700 mb-1">Address</label>
                                        <input
                                            type="text"
                                            value={formData.address}
                                            onChange={(e) => setFormData({ ...formData, address: e.target.value })}
                                            className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                        />
                                    </div>

                                    <div className="grid grid-cols-3 gap-4">
                                        <div>
                                            <label className="block text-sm font-medium text-gray-700 mb-1">City</label>
                                            <input
                                                type="text"
                                                value={formData.city}
                                                onChange={(e) => setFormData({ ...formData, city: e.target.value })}
                                                className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                            />
                                        </div>
                                        <div>
                                            <label className="block text-sm font-medium text-gray-700 mb-1">Country</label>
                                            <input
                                                type="text"
                                                value={formData.country}
                                                onChange={(e) => setFormData({ ...formData, country: e.target.value })}
                                                className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                            />
                                        </div>
                                        <div>
                                            <label className="block text-sm font-medium text-gray-700 mb-1">Postal Code</label>
                                            <input
                                                type="text"
                                                value={formData.postal_code}
                                                onChange={(e) => setFormData({ ...formData, postal_code: e.target.value })}
                                                className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                                            />
                                        </div>
                                    </div>

                                    {/* Risk Level - Auto-calculated */}
                                    <div className="bg-blue-50 border border-blue-200 rounded-md p-3 mb-4">
                                        <p className="text-xs text-blue-800">
                                            <span className="font-semibold">Note:</span> Risk level will be automatically calculated by the system based on screening results (PEP, sanctions, adverse media, etc.).
                                        </p>
                                    </div>

                                    {/* Checkboxes */}
                                    <div className="space-y-2">
                                        <label className="flex items-center">
                                            <input
                                                type="checkbox"
                                                checked={formData.is_pep}
                                                onChange={(e) => setFormData({ ...formData, is_pep: e.target.checked })}
                                                className="mr-2"
                                            />
                                            <span className="text-sm text-gray-700">PEP (Politically Exposed Person)</span>
                                        </label>
                                        <label className="flex items-center">
                                            <input
                                                type="checkbox"
                                                checked={formData.is_sanctioned}
                                                onChange={(e) => setFormData({ ...formData, is_sanctioned: e.target.checked })}
                                                className="mr-2"
                                            />
                                            <span className="text-sm text-gray-700">Sanctioned</span>
                                        </label>
                                    </div>
                                </div>
                            )}

                            {/* Step 2: KYC Verification */}
                            {currentStep === 2 && (
                                <div className="space-y-4">
                                    <div className="bg-blue-50 border border-blue-200 rounded-md p-3 mb-4">
                                        <p className="text-xs text-blue-800">
                                            <span className="font-semibold">KYC Verification:</span> Complete the verification checks below before adding the customer.
                                        </p>
                                    </div>

                                    {/* Source of Funds */}
                                    <div>
                                        <label className="block text-sm font-medium text-gray-700 mb-1">Source of Funds</label>
                                        <textarea
                                            value={kycData.source_of_funds}
                                            onChange={(e) => setKycData({ ...kycData, source_of_funds: e.target.value })}
                                            placeholder="Describe the source of funds..."
                                            className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 min-h-[80px]"
                                            rows="3"
                                        />
                                    </div>

                                    {/* Source of Wealth */}
                                    <div>
                                        <label className="block text-sm font-medium text-gray-700 mb-1">Source of Wealth</label>
                                        <textarea
                                            value={kycData.source_of_wealth}
                                            onChange={(e) => setKycData({ ...kycData, source_of_wealth: e.target.value })}
                                            placeholder="Describe the source of wealth..."
                                            className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 min-h-[80px]"
                                            rows="3"
                                        />
                                    </div>

                                    {/* Automatic Screening Info */}
                                    <div className="bg-green-50 border border-green-200 rounded-md p-3 mb-4">
                                        <p className="text-xs text-green-800">
                                            <span className="font-semibold">Automatic Screening:</span> The system will automatically check PEP databases, sanctions lists, adverse media, and criminal watchlists when you proceed to the review step. Risk level will be automatically calculated based on screening results.
                                        </p>
                                    </div>

                                    {isScreening && (
                                        <div className="bg-yellow-50 border border-yellow-200 rounded-md p-3 mb-4">
                                            <div className="flex items-center space-x-2">
                                                <svg className="w-5 h-5 text-yellow-600 animate-spin" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
                                                </svg>
                                                <p className="text-xs text-yellow-800">
                                                    <span className="font-semibold">Screening in progress...</span> Checking PEP databases, sanctions lists, adverse media, and criminal watchlists. Calculating risk level...
                                                </p>
                                            </div>
                                        </div>
                                    )}

                                    {/* Verification Notes */}
                                    <div>
                                        <label className="block text-sm font-medium text-gray-700 mb-1">Verification Notes</label>
                                        <textarea
                                            value={kycData.notes}
                                            onChange={(e) => setKycData({ ...kycData, notes: e.target.value })}
                                            placeholder="Add any additional verification notes..."
                                            className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 min-h-[80px]"
                                            rows="3"
                                        />
                                    </div>
                                </div>
                            )}

                            {/* Step 3: Review */}
                            {currentStep === 3 && (
                                <div className="space-y-4">
                                    <div className={`border rounded-md p-3 mb-4 ${screeningResults?.overall_status === 'BLOCK'
                                        ? 'bg-red-50 border-red-200'
                                        : screeningResults?.overall_status === 'REVIEW'
                                            ? 'bg-yellow-50 border-yellow-200'
                                            : 'bg-green-50 border-green-200'
                                        }`}>
                                        <p className={`text-xs ${screeningResults?.overall_status === 'BLOCK'
                                            ? 'text-red-800'
                                            : screeningResults?.overall_status === 'REVIEW'
                                                ? 'text-yellow-800'
                                                : 'text-green-800'
                                            }`}>
                                            <span className="font-semibold">Screening Status:</span> {
                                                screeningResults?.overall_status === 'BLOCK'
                                                    ? 'BLOCKED - Sanctions match found. Customer cannot be accepted.'
                                                    : screeningResults?.overall_status === 'REVIEW'
                                                        ? 'REVIEW REQUIRED - Matches found. Please review before accepting.'
                                                        : 'CLEAR - No matches found. Customer can be accepted.'
                                            }
                                        </p>
                                    </div>

                                    {/* Screening Results */}
                                    {screeningResults && (
                                        <div className="bg-gray-50 border border-gray-200 rounded-md p-4">
                                            <h4 className="text-sm font-semibold text-gray-900 mb-3">Automatic Screening Results</h4>
                                            <div className="space-y-2 text-xs">
                                                <div className="flex items-center justify-between">
                                                    <span className="text-gray-600">PEP Check:</span>
                                                    <span className={screeningResults.pep_match ? 'text-red-600 font-semibold' : 'text-green-600'}>
                                                        {screeningResults.pep_match ? '✗ Match Found' : '✓ Clear'}
                                                    </span>
                                                </div>
                                                <div className="flex items-center justify-between">
                                                    <span className="text-gray-600">Sanctions Check:</span>
                                                    <span className={screeningResults.sanctions_match ? 'text-red-600 font-semibold' : 'text-green-600'}>
                                                        {screeningResults.sanctions_match ? '✗ Match Found' : '✓ Clear'}
                                                    </span>
                                                </div>
                                                <div className="flex items-center justify-between">
                                                    <span className="text-gray-600">Adverse Media:</span>
                                                    <span className={screeningResults.adverse_media_match ? 'text-yellow-600 font-semibold' : 'text-green-600'}>
                                                        {screeningResults.adverse_media_match ? '⚠ Match Found' : '✓ Clear'}
                                                    </span>
                                                </div>
                                                <div className="flex items-center justify-between">
                                                    <span className="text-gray-600">Criminal Watchlist:</span>
                                                    <span className={screeningResults.criminal_match ? 'text-red-600 font-semibold' : 'text-green-600'}>
                                                        {screeningResults.criminal_match ? '✗ Match Found' : '✓ Clear'}
                                                    </span>
                                                </div>

                                                {/* Show matches if any */}
                                                {screeningResults.matches && screeningResults.matches.length > 0 && (
                                                    <div className="mt-3 pt-3 border-t border-gray-300">
                                                        <p className="font-semibold text-gray-700 mb-2">Matches Found:</p>
                                                        <div className="space-y-1 max-h-32 overflow-y-auto">
                                                            {screeningResults.matches.map((match, idx) => (
                                                                <div key={idx} className="bg-white border border-gray-200 rounded p-2">
                                                                    <p className="font-medium text-gray-800">{match.type}: {match.matched_name}</p>
                                                                    <p className="text-gray-600 text-xs">Source: {match.source}</p>
                                                                    {match.similarity && (
                                                                        <p className="text-gray-600 text-xs">Similarity: {(match.similarity * 100).toFixed(1)}%</p>
                                                                    )}
                                                                </div>
                                                            ))}
                                                        </div>
                                                    </div>
                                                )}
                                            </div>
                                        </div>
                                    )}

                                    {/* Risk Level - Auto-calculated */}
                                    {screeningResults && (
                                        <div className="bg-gray-50 border border-gray-200 rounded-md p-4">
                                            <h4 className="text-sm font-semibold text-gray-900 mb-3">Auto-Calculated Risk Assessment</h4>
                                            <div className="space-y-2 text-xs">
                                                <div className="flex items-center justify-between">
                                                    <span className="text-gray-600">Risk Level:</span>
                                                    <span className={`font-semibold ${screeningResults.calculated_risk_level === 'CRITICAL' ? 'text-red-700' :
                                                        screeningResults.calculated_risk_level === 'HIGH' ? 'text-red-600' :
                                                            screeningResults.calculated_risk_level === 'MEDIUM' ? 'text-yellow-600' :
                                                                'text-green-600'
                                                        }`}>
                                                        {screeningResults.calculated_risk_level || 'LOW'}
                                                    </span>
                                                </div>
                                                {screeningResults.risk_score !== undefined && (
                                                    <div className="flex items-center justify-between">
                                                        <span className="text-gray-600">Risk Score:</span>
                                                        <span className="text-gray-900 font-mono">
                                                            {(screeningResults.risk_score * 100).toFixed(1)}%
                                                        </span>
                                                    </div>
                                                )}
                                                {screeningResults.risk_factors && Object.keys(screeningResults.risk_factors).length > 0 && (
                                                    <div className="mt-3 pt-3 border-t border-gray-300">
                                                        <p className="font-semibold text-gray-700 mb-2">Risk Factors:</p>
                                                        <div className="space-y-1">
                                                            {Object.entries(screeningResults.risk_factors).map(([factor, value]) => (
                                                                <div key={factor} className="flex items-center justify-between">
                                                                    <span className="text-gray-600 capitalize">{factor.replace('_', ' ')}:</span>
                                                                    <span className="text-gray-900">{(value * 100).toFixed(1)}%</span>
                                                                </div>
                                                            ))}
                                                        </div>
                                                    </div>
                                                )}
                                            </div>
                                        </div>
                                    )}

                                    {/* Customer Information Summary */}
                                    <div className="bg-gray-50 border border-gray-200 rounded-md p-4">
                                        <h4 className="text-sm font-semibold text-gray-900 mb-3">Customer Information</h4>
                                        <div className="grid grid-cols-2 gap-3 text-xs">
                                            <div>
                                                <span className="font-medium text-gray-600">Customer ID:</span>
                                                <p className="text-gray-900 font-mono">{formData.customer_id || 'Will be auto-generated'}</p>
                                            </div>
                                            <div>
                                                <span className="font-medium text-gray-600">Type:</span>
                                                <p className="text-gray-900">{formData.customer_type}</p>
                                            </div>
                                            {formData.customer_type === 'INDIVIDUAL' ? (
                                                <>
                                                    <div>
                                                        <span className="font-medium text-gray-600">Name:</span>
                                                        <p className="text-gray-900">{formData.first_name} {formData.last_name}</p>
                                                    </div>
                                                    <div>
                                                        <span className="font-medium text-gray-600">Email:</span>
                                                        <p className="text-gray-900">{formData.email || 'N/A'}</p>
                                                    </div>
                                                </>
                                            ) : (
                                                <>
                                                    <div>
                                                        <span className="font-medium text-gray-600">Company:</span>
                                                        <p className="text-gray-900">{formData.company_name || 'N/A'}</p>
                                                    </div>
                                                    <div>
                                                        <span className="font-medium text-gray-600">Email:</span>
                                                        <p className="text-gray-900">{formData.email || 'N/A'}</p>
                                                    </div>
                                                </>
                                            )}
                                            <div>
                                                <span className="font-medium text-gray-600">Risk Level:</span>
                                                <p className="text-gray-900 font-mono">
                                                    {screeningResults?.calculated_risk_level || 'Will be auto-calculated'}
                                                </p>
                                            </div>
                                        </div>
                                    </div>


                                    {/* Error Message */}
                                    {createCustomerMutation.isError && (
                                        <div className="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded text-sm">
                                            {createCustomerMutation.error?.response?.data?.detail ||
                                                createCustomerMutation.error?.message ||
                                                'Failed to create customer. Please try again.'}
                                        </div>
                                    )}
                                </div>
                            )}
                        </div>

                        {/* Footer - Static */}
                        <div className="bg-white border-t border-gray-200 shadow px-3 py-2 flex justify-between space-x-3 flex-shrink-0">
                            <div>
                                {currentStep > 1 && (
                                    <button
                                        type="button"
                                        onClick={() => setCurrentStep(currentStep - 1)}
                                        className="px-3 py-1.5 text-xs font-medium text-gray-700 bg-white border border-gray-300 rounded-md hover:bg-gray-50"
                                    >
                                        Previous
                                    </button>
                                )}
                            </div>
                            <div className="flex space-x-3">
                                <button
                                    type="button"
                                    onClick={async () => {
                                        // If customer was created but not accepted, delete it
                                        if (tempCustomerId) {
                                            try {
                                                await axios.delete(`${base_url}/customers/${tempCustomerId}/`)
                                            } catch (error) {
                                                console.error('Failed to delete temporary customer:', error)
                                            }
                                        }
                                        resetModal()
                                        setIsModalOpen(false)
                                    }}
                                    className="px-3 py-1.5 text-xs font-medium text-gray-700 bg-white border border-gray-300 rounded-md hover:bg-gray-50"
                                >
                                    Cancel
                                </button>
                                {currentStep < 3 ? (
                                    <button
                                        type="button"
                                        onClick={async () => {
                                            // Validate current step before proceeding
                                            if (currentStep === 1) {
                                                // Validate customer info
                                                if (!formData.email) {
                                                    alert('Please fill in Email')
                                                    return
                                                }
                                                if (formData.customer_type === 'INDIVIDUAL' && (!formData.first_name || !formData.last_name)) {
                                                    alert('Please fill in First Name and Last Name')
                                                    return
                                                }
                                                if (formData.customer_type === 'CORPORATE' && !formData.company_name) {
                                                    alert('Please fill in Company Name')
                                                    return
                                                }

                                                // Create customer first
                                                try {
                                                    await createCustomerMutation.mutateAsync(formData)
                                                } catch (error) {
                                                    alert('Failed to create customer. Please check the form.')
                                                    return
                                                }

                                                setCurrentStep(2)
                                            } else if (currentStep === 2) {
                                                // Move to review and trigger automatic screening
                                                if (!tempCustomerId) {
                                                    alert('Customer not created. Please go back to step 1.')
                                                    return
                                                }

                                                setCurrentStep(3)
                                                setIsScreening(true)

                                                // Trigger automatic screening
                                                try {
                                                    await screenCustomerMutation.mutateAsync(tempCustomerId)
                                                } catch (error) {
                                                    console.error('Screening error:', error)
                                                    setIsScreening(false)
                                                }
                                            }
                                        }}
                                        className="px-3 py-1.5 text-xs font-medium text-white bg-blue-600 rounded-md hover:bg-blue-700"
                                    >
                                        {currentStep === 1 ? 'Next: KYC Verification' : 'Next: Review & Screening'}
                                    </button>
                                ) : (
                                    <>
                                        <button
                                            type="button"
                                            onClick={(e) => {
                                                e.preventDefault()
                                                if (!tempCustomerId) {
                                                    alert('Customer ID not found')
                                                    return
                                                }

                                                const reason = prompt('Please provide a reason for rejection:')
                                                if (reason) {
                                                    rejectCustomerMutation.mutate({
                                                        customerId: tempCustomerId,
                                                        reason: reason
                                                    })
                                                }
                                            }}
                                            disabled={rejectCustomerMutation.isLoading || screeningResults?.overall_status === 'BLOCK'}
                                            className="px-3 py-1.5 text-xs font-medium text-white bg-red-600 rounded-md hover:bg-red-700 disabled:opacity-50 disabled:cursor-not-allowed"
                                        >
                                            {rejectCustomerMutation.isLoading ? 'Rejecting...' : 'Reject Customer'}
                                        </button>
                                        <button
                                            type="button"
                                            onClick={(e) => {
                                                e.preventDefault()
                                                if (!tempCustomerId) {
                                                    alert('Customer ID not found')
                                                    return
                                                }

                                                if (screeningResults?.overall_status === 'BLOCK') {
                                                    alert('Cannot accept customer with sanctions match. Customer is blocked.')
                                                    return
                                                }

                                                acceptCustomerMutation.mutate(tempCustomerId)
                                            }}
                                            disabled={acceptCustomerMutation.isLoading || screeningResults?.overall_status === 'BLOCK' || isScreening}
                                            className="px-3 py-1.5 text-xs font-medium text-white bg-green-600 rounded-md hover:bg-green-700 disabled:opacity-50 disabled:cursor-not-allowed"
                                        >
                                            {acceptCustomerMutation.isLoading ? 'Accepting...' : 'Accept Customer'}
                                        </button>
                                    </>
                                )}
                            </div>
                        </div>
                    </div>
                </div>
            )}
        </div>
    )
}

export default CustomerList
